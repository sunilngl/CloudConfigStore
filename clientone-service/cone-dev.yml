service:
  greeting: Hello from cone-dev
  
server:
port: 3002

spring:
  data:
    cassandra:
      keyspace-name: member_keyspace
      port: 9042
      contact-points: localhost
      username: cassandra
      password: cassandra
